import { createActions } from 'reflux';

const layoutActions = createActions({
  toggleCharts: {},
  toggleFilters: {},
  toggleFacilities: {},
  toggleDataset: {},
  toggleYear: {},
});

export default layoutActions;
